controller:
  nodeSelector:
    kubernetes.io/os: linux
  admissionWebhooks:
    patch:
      nodeSelector:
        kubernetes.io/os: linux
  service:
    annotations:
      service.beta.kubernetes.io/aws-load-balancer-type: nlb
    type: NodePort
    nodePorts:
      http: 32080
      https: 32443

defaultBackend:
  nodeSelector:
    kubernetes.io/os: linux

